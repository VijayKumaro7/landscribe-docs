# Netlify SPA fallback: serve index.html for client-side routes
# (/library, etc.). Real files (documents/*.pdf, fonts/*, assets/*) are
# matched first by Netlify, so this only applies to unknown paths.
/*  /index.html  200
